#-*-encoding=utf-8-*-
1. 设计一个栈，要求push、pop、max、min四个接口的时间复杂度为O(1).
2. 字符串的子串替换，要求不能使用字符串函数. char *str_replace(char *src, const char *from, const char *to);
3. 谈谈对面向对象编程的认识(q3)
4. 数据库1中存放a类数据，数据库2存放着以天为单位划分的表30张（如table_20110909, table_20110910），总共是一个月的数据，
   数据库1中的a类数据中有一个字段userid来判别唯一用户身份，数据库2中的30张表（每张表结构相同）也有一个字段userid来唯一
   识别用户，如何判定a类数据库的多少用户在数据库2中出现？
